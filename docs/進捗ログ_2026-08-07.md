# 同人誌・NovelAI関連 進捗ログ 2026-08-07

## 1. bikiobrainから同人誌関係ファイルを収集
- 「同人誌の漫画を作ろう企画」の相談ログ・検討メモをbikiobrainから探し出し、`douzinsi`直下にコピー
- 当初は買取案件のGoogle Docsも混ざっていたが、目的が違う（企画・量産の話であり、買取案件ではない）と指摘を受けて除外
- AI会話ログの大きなファイル（他の話題と混在）から、同人誌関連セッションだけを抽出して個別ファイル化
- Downloadsにあった`同人誌AI制作_手順まとめ.md`も回収

## 2. 開発中プロジェクト（novelai-batch）の取り込み
- `~/Desktop/novelai-batch/`（NovelAI API量産スクリプト＋Flask簡易Webアプリ）を丸ごとdouzinsiにコピー
- もう一つの実行環境`~/Desktop/.../ComfyUI`（27GB、ほぼモデルデータのみ）は独自開発物なしのためコピー対象外と判断

## 3. GitHubリポジトリ化・公開
- GitHub CLI（`gh`）をインストールし、デバイスフローでこのMacをユーザーのGitHubアカウント（`himeshou2929`）にログイン
- `.env`・生成画像・ログを`.gitignore`で除外し、秘密情報が紛れていないか事前スキャンした上でコミット
- `himeshou2929/douzinsi`として公開リポジトリを作成・プッシュ
  - クローンURL: `https://github.com/himeshou2929/douzinsi.git`

## 4. 共同開発者（たかとし氏）のリポジトリへのアクセス試行
- `taka01150810/adult-image`のクローンを試みるも、非公開リポジトリでコラボレーター未招待のため失敗（404）
- ログイン済みでも招待されていなければアクセスできない仕組みであることを確認・説明
- 結論：たかとし氏に`himeshou2929`をコラボレーター招待してもらう必要あり（未解決、次回への持ち越し）

## 5. novelai-batchをFlask/HTML-CSS/NovelAI APIスタックで再構成
- `config.py`新設（環境変数・APIデフォルト値を一元管理）
- `novelai_client.py`のエラーハンドリング強化（`NovelAIError`）
- インラインCSSを`static/css/style.css`に分離、`templates/base.html`で共通レイアウト化
- CLIバッチ版（`generate.py`）もWeb版と同じクライアントを共有するよう統一
- `.env.example`・`novelai-batch/README.md`を追加

## 6. リポジトリ構成の整理
- ルートにあった無関係なファイル（`index.html`・`assets/` = Claudeヘルプセンター記事のキャッシュ、プロジェクトと無関係）を削除
- `bikiobrain-同人誌/` → `docs/`にリネームし、企画資料とコードを明確に分離
- ルートに全体像を説明する`README.md`を新規追加
- 「共同開発者はエンジニアなので構成をしっかり」という要望に対応

## 7. ローカル実行環境の起動
- `http://127.0.0.1:5050`でFlaskアプリを起動（バックグラウンド常駐）
  - `/` : 1枚だけ生成
  - `/batch` : まとめて生成（空行区切りで複数シーン）

## 8. キャラクター・スタイル固定機能の追加（Vibe Transfer）
- 「生成のたびにキャラが固定されない」という課題に対応
- NovelAI公式のVibe Transfer仕様を調査（参照画像は最大16枚まで同時使用可）した上で実装
- 「キャラ基準画像」「スタイル基準画像」を別々に指定可能に（両方同時使用も可、それぞれ独立した参照強度）
- デフォルト参照強度: キャラ0.6 / スタイル0.3（NovelAI公式推奨の「強度合計1.0以下」の目安に沿う）

## 9. 生成枚数指定機能の追加
- `n_samples`（1〜8枚）を`/`・`/batch`両画面に追加
- `novelai_client.py`の関数を`generate_image`→`generate_images`にリネームし、常にリスト（複数枚）を返す設計に統一
- `/batch`では「シーン数×指定枚数」だけ生成される旨を画面に明記

## 10. 運用ルールの確立
- 「コミット・プッシュは事前に内容を報告し、許可を得てから実行する」というルールを合意・記憶化
- 以降、全てのコミット前に秘密情報混入がないか差分をスキャンしてから実行する運用を徹底

## コミット履歴（本日分）
1. `同人誌プロジェクト初期コミット`
2. `novelai-batch: Flask/HTML-CSS/NovelAI APIスタックで構成を整理`
3. `リポジトリ構成を整理`
4. `novelai-batch: スタイル固定・生成枚数指定に対応`

## 現在地・次回やること
1. たかとし氏に`himeshou2929`をコラボレーター招待してもらう（`adult-image`リポジトリへのアクセス待ち）
2. Vibe Transferの実際の生成テスト（キャラ固定・スタイル固定の精度確認）は未実施、トークンを使った実通信での動作確認が必要
3. より高精度なキャラ固定が必要な場合はLoRA学習（ComfyUI側）を検討
