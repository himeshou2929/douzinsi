# 同人誌・NovelAI関連 進捗ログ 2026-08-04

## 1. 既存プロジェクトの確認
- `検討メモ_2026-07-17.md`を再確認。方針：DLsite/FANZA向け電子販売、キャラは成人と明確にわかる設定が絶対条件
- 直近の別ライン（2026-07-28）では、露骨な性描写を避けた全年齢向けファンタジー路線（狐耳の女の子・キャンプ→旅→村→山頂→星空の10シーン）にComfyUI(Z-Image Turbo)で転換して進行中と判明

## 2. NovelAIの実地検証（クラウドChrome経由）
- `novelai.net/stories`と`/image`にアクセスし、UI・料金体系を確認
- **Stories（テキスト生成）**：無料トライアル50回まで、ジャンル選択式は無くメモリー/作者のメモ/ロアーブックで制御。プランはTablet $10・Scroll $15・Opus $25
- **画像生成**：Stable Diffusionライセンス同意が必須。「4-koma manga」指定でも厳密なコマ割りにはならず1枚絵止まり
- **吹き出し**：「speech bubble」タグで空白形状は出るがセリフは描画されない（Stable Diffusion系共通の弱点）
- 結論：検討メモの「絵はAI・セリフは別工程(Pillow等)で後付け」方針がNovelAIでも裏付けられた

## 3. たかとし氏との新規共同案件「adult-image」
- 目的：NSFW画像生成＋販売素材の自動作成パイプライン、月3タイトル販売目標（DMM同人等）
- 技術構成：Python/FastAPI＋React/Next.js＋LangChain、まずNovelAI APIから検証する方針
- リポジトリ：`github.com/taka01150810/adult-image`（今回はセットアップ見送り）
- **コンテンツ境界を改めて確認**：環境構築・API接続・スクリプト設計などの技術支援は対応可能。露骨な性的表現を含むプロンプトの作成・生成実行はClaude側で対応不可、という前提を再確認・維持

## 4. NovelAI API量産スクリプトの作成・検証
- `~/Desktop/novelai-batch/`に環境構築
  - `.env`（APIトークン、一度上書きあり→再設定済み）
  - `prompts.csv`（プロンプト一覧）
  - `generate.py` / `novelai_client.py`（API呼び出しロジック）
- 実行結果：**`400 Recaptcha token is required for trial generation`** のエラーで生成失敗
  - 原因：無料トライアルアカウントがAPIエンドポイントを直接叩くとreCAPTCHA認証が要求される仕組み
  - 内容の性的/非性的にかかわらず、トライアル中は自動化そのものがブロックされることが判明
  - 有料プラン加入で制限が外れる可能性が高いが未検証

## 5. 実際に生成したい2枚のプロンプト依頼と対応方針
- 露骨な性的表現を含む2枚分のプロンプトが提示されたが、生成実行はClaude側でお断り
- 対応方針を再確認：性的表現を含む生成の実行・自動化はNG、非性的な範囲での量産や技術サポートはOKという線引きで合意

## 6. ブラウザから使える簡易Webアプリの作成
- `~/Desktop/novelai-batch/`にFlaskアプリを追加
  - `app.py`・`templates/index.html`・`novelai_client.py`（`generate_image`関数を共通化）
  - `http://127.0.0.1:5050` でローカル起動、プロンプト入力→生成→画面表示のシンプルなUI
- 「ブラウザに埋め込んで代行できないか」という相談があったが、実装の見た目を変えても性的コンテンツの自動生成を代行することはできない旨を説明し、非性的な範囲での実装として合意
- 動作確認中、テキストエリアへの**コピー＆ペーストができない**不具合を報告あり（原因切り分け中、ページ側にJS等の制限は無いためブラウザ/OS側の要因を確認中）

## 現在地・次回やること
1. コピペ不具合の原因切り分け（ブラウザ種別・貼り付け時の挙動を確認中）
2. NovelAI有料プランに加入するかどうかの判断（加入すればrecaptcha制限が外れるか要検証）
3. 量産する場合のコンテンツ方向性の最終決定（既存の同人誌ファンタジー路線 or 新規非性的テーマ）
4. adult-imageプロジェクト（たかとし氏案件）は今回リポジトリ着手を見送り、必要になれば別途仕切り直し
